export const selectCurrentId = (state) => state.appState.currentId;

export const selectRefreshTodosFlag = (state) => state.appState.refreshTodosFlag;

export const selectSearchedTodo = (state) => state.appState.searchedTodo;

export const selectIsSortFlag = (state) => state.appState.isSortFlag;
